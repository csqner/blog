{
    "swagger": "2.0",
    "info": {
        "description": "{{.Description}}",
        "title": "{{.Title}}",
        "contact": {},
        "license": {},
        "version": "{{.Version}}"
    },
    "host": "{{.Host}}",
    "basePath": "{{.BasePath}}",
    "paths": {
        "/user/test": {
            "get": {
                "description": "Add a new user",
                "tags": [
                    "user"
                ],
                "summary": "Add new user to the database",
                "responses": {
                    "200": {}
                }
            }
        }
    }
}